// Per-app configuration for the accessibility report generator
// (see src/test-utils/a11y/reporter.js). `overrides` can re-tag any WCAG criterion
// for this app's reality; the generic catalogue already fits, so none are needed.
module.exports = {
  projectName: 'rn-warrendeleon',
  overrides: {},
};
